//This class is for the log that extends from Car.
class Log extends Car {
  
  //The Log function with 5 parameters: an x, y, w, h, and s.
  //Super constructor is also declared.
  Log(float x, float y, float w, float h, float s) {
    super(x, y, w, h, s);
  }
}